configurations {
    deps
}

repositories {
    ivy { url "http://localhost:8090/ivy" }
    maven { url "http://localhost:8090/maven" }
}

dependencies {
    deps 'com.gradlesummit:aa:1.0'
}

task printDeps << {
    configurations.deps.files.each {
        println it.name
    }
}
