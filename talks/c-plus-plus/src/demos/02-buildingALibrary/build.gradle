apply plugin: 'cpp-lib'
apply plugin: 'cpp'

cpp {
    sourceSets {
        test {
            libs << libraries.main
        }
    }
}

executables {
    test {
        sourceSets << cpp.sourceSets.test
    }
}
