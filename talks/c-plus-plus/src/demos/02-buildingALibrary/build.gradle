apply plugin: 'cpp'

libraries {
    greeting {
        binaries.withType(SharedLibraryBinary) {
            cppCompiler.define 'DLL_EXPORT'
        }
    }
}

sources {
    helloWorld {
        cpp {
            lib libraries.greeting
        }
    }
}

executables {
    helloWorld
}
