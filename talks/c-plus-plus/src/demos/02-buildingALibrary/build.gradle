apply plugin: 'cpp-lib'
apply plugin: 'cpp'

cpp {
    sourceSets {
        test {
            lib libraries.main
        }
    }
}

libraries {
    main {
        binaries.all {
            define 'DLL_EXPORT'
        }
    }
}

executables {
    test {
        source cpp.sourceSets.test
    }
}
