apply plugin: 'cpp-lib'
apply plugin: 'cpp'

libraries {
    main {
        binaries.all {
            define 'DLL_EXPORT'
        }
    }
}

cpp {
    sourceSets {
        test {
            lib libraries.main
        }
    }
}

executables {
    test {
        source cpp.sourceSets.test
    }
}
