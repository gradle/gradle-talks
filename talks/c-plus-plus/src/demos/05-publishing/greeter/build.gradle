apply plugin: 'cpp-lib'

group 'org.gradle.demo'
version '1.0'

libraries {
    main {
        binaries.all {
            define 'DLL_EXPORT'
        }
    }
}

uploadArchives {
    repositories {
        ivy {
            url "../repo"
        }
    }
}
